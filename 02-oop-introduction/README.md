# 🏗️ Módulo: Introdução à Programação Orientada a Objetos (POO)

Este módulo marca a transição da programação estruturada para o paradigma de Orientação a Objetos. O foco foi aprender a modelar sistemas reais através de classes, garantindo o reaproveitamento de código e a segurança dos dados.

## 💻 Tópicos Estudados:

### 1. Fundamentos de POO
- **Classes e Atributos:** Criação de moldes para representar entidades (ex: Triângulo, Produto).
- **Métodos e Delegação:** Transferência da lógica de cálculo para dentro da classe, promovendo o reaproveitamento.
- **Classe Object:** Entendimento do método `toString` para representação textual de objetos.
- **Membros Estáticos:** Uso da palavra-chave `static` para membros que pertencem à classe e não às instâncias (ex: calculadoras ou conversores).

### 2. Ciclo de Vida e Proteção de Dados
- **Construtores:** Mecanismos para obrigar a inicialização de atributos no momento da criação do objeto.
- **Palavra `this`:** Referência à própria instância para diferenciar atributos de variáveis locais.
- **Sobrecarga:** Disponibilização de mais de uma versão de um mesmo construtor ou método para maior flexibilidade.

### 3. Pilares de Segurança
- **Encapsulamento:** Proteção do estado interno do objeto.
- **Getters e Setters:** Métodos controlados para acessar e modificar atributos privados.
- **Modificadores de Acesso:** Definição de visibilidade (`private`, `public`, `protected`, `default`).

## 🚀 Conceitos Técnicos Aplicados:
- **Resolução de problemas sem vs. com POO:** Comparação prática de como a POO reduz a repetição de código.
- **Gerenciamento de Estado:** Controle de estoque e atualizações de produtos através de métodos internos.

---
*Este módulo consolidou a base necessária para entender arquiteturas de software mais complexas e frameworks como o Spring Boot.*